<h2>Subjects</h2>
<p>
    Customize a list of subjects relevant to your school. These will be used when assigning interventions, and referrals to track behavior patterns in different subjects.
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody data-bind="foreach : subjects">
        <tr>
            <td data-bind="text : Name"></td>
            <td><a href="#" data-bind="click : $parent.deltet">delete</a></td>
        </tr>
    </tbody>
</table>
<div class="input-append">
              <input class="span2" data-bind="value : nameToAdd" size="16" type="text">
              <button class="btn" data-bind="click: add" type="button">Add</button>
            </div>